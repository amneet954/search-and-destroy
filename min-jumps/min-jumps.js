"use strict";

// Complete this algo
const minJumps = arr => {
  console.log(arr);

  let jumpLength = arr[0];
  let maxDistance = 0;
  let currentSpot = 0;
  let nextPossibleJump = array.slice(currentSpot + 1, jumpLength);
  console.log("dfjngds", nextPossibleJump);
};

module.exports = minJumps;
